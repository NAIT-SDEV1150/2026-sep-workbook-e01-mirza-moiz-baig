{
  "name": "lesson-19-homework",
  "private": true,
  "type": "module",
  "scripts": {
    "api": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
