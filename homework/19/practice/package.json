{
  "name": "lesson-20-homework",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "api": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "json-server": "^1.0.0-beta.3"
  }
}
