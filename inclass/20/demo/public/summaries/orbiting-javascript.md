# Orbiting JavaScript

This guide treats JavaScript as a small system of connected parts. Readers move from values and functions into browser events, network requests, and DOM updates.

## Why it fits the catalog

- It explains `fetch()` as one step in a larger browser workflow.
- It uses short examples that can be tested quickly.
- It connects API responses to visible page changes.
