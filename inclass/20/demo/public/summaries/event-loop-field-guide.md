# The Event Loop Field Guide

This book follows browser work as it waits, finishes, and updates the page. It keeps asynchronous code practical by tracing each promise from request to response.

## Reader notes

Students use it to connect `async` functions, `await`, and user feedback. The examples are small enough to discuss line by line.
